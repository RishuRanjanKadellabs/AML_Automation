import Milestone1DedupScreeningLocators from "../../milestone1/objectrepositories/DedupScreeningLocators";

/**
 * Reuses milestone1 Dedup Screening locators — no duplicate selector strings.
 * Source Excel: pipeline/test-data/Risk Assesment Test Cases.xlsx (DDS-TC rows).
 */
const DedupScreeningLocators = {
  ...Milestone1DedupScreeningLocators,
};

export default DedupScreeningLocators;
